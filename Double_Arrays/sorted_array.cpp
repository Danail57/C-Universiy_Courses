#include <iostream>
using namespace std;

int main()
{
	int sorted_array_A;
	cout << "Enter how many numbers you want: ";
	cin >> sorted_array_A;

	int* arr = new int[sorted_array_A];
	cout << "Enter the elements is ascending order: ";
	int current_number = 0;

	for (int i = 0; i < sorted_array_A; i++)
}
